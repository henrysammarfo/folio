import { createFileRoute } from "@tanstack/react-router"; import { PublicShell } from "@/components/public-page"; import { StatusBadge } from "@/components/folio-brand";
export const Route=createFileRoute("/network")({head:()=>({meta:[{title:"Network Truth — FOLIO"},{name:"description",content:"Exact network availability across the FOLIO stack."},{property:"og:title",content:"Network Truth — FOLIO"},{property:"og:description",content:"Exact network availability across the FOLIO stack."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});
const rows=[["Real xStocks · NestUSD · Kamino market","Mainnet only","amber"],["Jupiter quote · Pyth · pool reads","Mainnet read","green"],["FOLIO policy · Token-2022 harness","Devnet","blue"],["Jupiter / Kamino CPI","Local fork","blue"],["Broadcast execution","Not funded","neutral"]] as const;
function Page(){return <PublicShell eyebrow="Network matrix" title="No blurred lines between demo and live." intro="Every capability is labeled by where it actually runs. Read access is not execution, and fork proof is not a mainnet fill."><div className="network-table">{rows.map(([name,status,tone])=><div key={name}><span>{name}</span><StatusBadge tone={tone}>{status}</StatusBadge></div>)}</div></PublicShell>}