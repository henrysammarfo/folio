import { createFileRoute } from "@tanstack/react-router"; import { useState } from "react"; import { DeskShell, Panel } from "@/components/desk-shell"; import { Switch } from "@/components/ui/switch"; import { StatusBadge } from "@/components/folio-brand";
export const Route=createFileRoute("/desk/settings")({head:()=>({meta:[{title:"Settings — FOLIO"},{name:"description",content:"Configure local FOLIO desk preferences."},{property:"og:title",content:"Settings — FOLIO"},{property:"og:description",content:"Configure local FOLIO desk preferences."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});
function Page(){const [alerts,setAlerts]=useState(true);const [strict,setStrict]=useState(true);return <DeskShell eyebrow="Local preferences" title="Settings"><div className="desk-grid"><Panel title="Network mode" meta={<StatusBadge tone="green">Safe default</StatusBadge>}><div className="setting-row"><span><b>Mainnet read</b><small>Observe live market and ledger data</small></span><StatusBadge tone="green">Selected</StatusBadge></div><div className="setting-row"><span><b>Broadcast</b><small>Requires funding and explicit enablement</small></span><StatusBadge tone="neutral">Unavailable</StatusBadge></div></Panel><Panel title="Policy preferences"><label className="setting-row"><span><b>Corporate-action alerts</b><small>Surface multiplier and distribution changes</small></span><Switch checked={alerts} onCheckedChange={setAlerts}/></label><label className="setting-row"><span><b>Strict fail-closed mode</b><small>Stop when any required signal is unresolved</small></span><Switch checked={strict} onCheckedChange={setStrict}/></label></Panel></div></DeskShell>}